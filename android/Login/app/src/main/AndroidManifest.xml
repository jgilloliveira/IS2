<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="is.proyecto.login">

    <!-- Internet permission -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UsuarioActivity" />
        <activity android:name=".DatoUserActivity" />
        <activity android:name=".MenuSrumActivity" />
        <activity android:name=".ScrumActivity" />
        <activity android:name=".ProyectoActivity" />
        <activity android:name=".GrupoActivity" />
        <activity android:name=".DatoSprintActivity" />
        <activity android:name=".SprintActivity" />
        <activity android:name=".USActivity" />
        <activity android:name=".DatoUSActivity" />
        <activity android:name=".MenuMiembroEquipoActivity" />
        <activity android:name=".SprintUsuarioActivity" />
        <activity android:name=".USUsuarioActivity" />
        <activity android:name=".MenuClienteActivity" />
        <activity android:name=".SprintProyectoActivity" />
        <activity android:name=".USProyectoActivity"></activity>
    </application>

</manifest>